# Byte-compiled / cached
__pycache__/
*.py[cod]
*$py.class
*.so

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
.eggs/

# Virtual environments
venv/
.venv/
env/
ENV/

# Notebooks (dev only — runtime data lives in /data at the repo root)
notebooks/

# Pipeline-only code (not used by the streamlit app)
constants.py
run_ticker_map_and_metadata.py

# ticker_mapping: ignore everything except combined_mappings.csv,
# which the deployed app loads at runtime
ticker_mapping/*
!ticker_mapping/fuzzy_matching/
ticker_mapping/fuzzy_matching/*
!ticker_mapping/fuzzy_matching/combined_mappings.csv

# Environment / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Editors / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# Testing / type-checking caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Streamlit
.streamlit/secrets.toml

# Logs
*.log

.spark-staging/
